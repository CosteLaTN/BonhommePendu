<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Jeu du Pendu"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity android:name=".AcceuilActivity"
            android:label="Jeu du Pendu"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PenduActivity" />
        <activity android:name=".StatutActivity" />
        <activity android:name=".HistoriqueActivity" />
        <activity android:name=".PreferencesActivity" />
        <activity android:name=".DictionnaireActivity" />

    </application>

</manifest>
